// This file can be replaced during build by using the `fileReplacements` array.
// `ng build` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: true,
  // Your web app's Firebase configuration
  firebaseConfig:{
    apiKey: "AIzaSyBU-Sm0rAUxDMTkaO29hSnGP5TEEkjVCMI",
    authDomain: "ovs-candidates.firebaseapp.com",
    databaseURL: "https://ovs-candidates-default-rtdb.firebaseio.com",
    projectId: "ovs-candidates",
    storageBucket: "ovs-candidates.firebasestorage.app",
    messagingSenderId: "926981495110",
    appId: "1:926981495110:web:6b03d7314a71c9b627ad47"
  }

};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/plugins/zone-error';  // Included with Angular CLI.
